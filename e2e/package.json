{
  "name": "@futureagi/e2e",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit",
    "catalog": "node scripts/flow-catalog.mjs",
    "catalog:check": "node scripts/flow-catalog.mjs --check",
    "coverage": "node scripts/e2e-coverage.mjs",
    "selftest": "node --test 'scripts/**/*.test.mjs'"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.0",
    "pg": "^8.13.0",
    "typescript": "^5.6.0"
  }
}
